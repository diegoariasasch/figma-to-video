{
  "name": "Frame to Video",
  "id": "frame-to-video-exporter",
  "api": "1.0.0",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": ["figma"],
  "networkAccess": {
    "allowedDomains": [
      "https://gist.githubusercontent.com",
      "https://raw.githubusercontent.com",
      "https://unpkg.com"
    ],
    "reasoning": "Kill-switch fetches a small JSON status file the maintainer hosts on GitHub (disabled flag). unpkg.com serves the ffmpeg.wasm converter loaded on demand when a MOV / ProRes file is dropped, so the plugin can transcode to WebM + VP9 alpha in-browser instead of requiring a CLI step."
  }
}
